export const CTA_REDIRECTS: Record<string, string> = {
  // Example: Hero CTA slug -> current destination URL/path
  // Keep this slug stable in admin (e.g. /go/home-hero)
  "home-hero": "/stories",
}

export const DEFAULT_CTA_REDIRECT = "/"
